<?php

// NOTE: this file REPLACES the stock config/auth.php from a fresh
// `laravel new` install (same pattern as app/Http/Kernel.php - see
// README). It keeps every stock Laravel 10 default untouched and only
// adds the "admin" guard + "admins" provider used to protect the
// Blade back-office panel (routes/web.php) with auth:admin, separate
// from the merchant-facing API (X-API-KEY, see AuthenticateMerchant).

return [

    'defaults' => [
        'guard' => 'web',
        'passwords' => 'users',
    ],

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],

        'admin' => [
            'driver' => 'session',
            'provider' => 'admins',
        ],
    ],

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => App\Models\User::class,
        ],

        'admins' => [
            'driver' => 'eloquent',
            'model' => App\Models\Admin::class,
        ],
    ],

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],

        'admins' => [
            'provider' => 'admins',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    'password_timeout' => 10800,

];
